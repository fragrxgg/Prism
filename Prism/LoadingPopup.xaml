<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prism"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        x:Name="LoadingScreen" x:Class="Prism.LoadingPopup"
        mc:Ignorable="d"
        Background="Transparent"
        Title="LoadingPopup" Height="204" Width="379"
        ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Topmost="True" WindowStyle="None"
        AllowsTransparency="True">

    <Border Background="#1A1A1A"
            CornerRadius="3"
            Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              BlurRadius="30"
                              ShadowDepth="0"
                              Opacity="0.8"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock
                       Foreground="#888888"
                       FontSize="{DynamicResource Lab.FontSize.Large}"
                       ><Run Language="it-it" Text="RAW File Elaborating" FontSize="10"/></TextBlock>
        </StackPanel>
    </Border>
</Window>