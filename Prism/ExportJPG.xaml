<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Prism"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" 
    x:Class="Prism.ExportJPG"
    mc:Ignorable="d"
    Title="Export JPG" 
    Height="233" 
    Width="480" 
    ResizeMode="NoResize" 
    WindowStartupLocation="CenterScreen"
    Background="{DynamicResource Lab.Brush.Background}" Icon="/export0.png">

    <Grid Margin="20,20,20,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="307*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Export Folder -->
        <Label Grid.Row="0" Grid.Column="0" 
               Content="Export File:" 
               VerticalAlignment="Center"
               Padding="0"/>

        <TextBox Grid.Row="0" Grid.Column="2" 
                 x:Name="txtExportFolder"
                 VerticalAlignment="Center"
                 Height="28"
                 VerticalContentAlignment="Center"
                 Padding="8,0"/>

        <Button Grid.Row="0" Grid.Column="4" 
                Content="..." 
                Width="40" 
                Height="28"
                VerticalAlignment="Center" Click="onOpenSaveDialog"/>

        <!-- Quality -->
        <Label Grid.Row="2" Grid.Column="0" 
               Content="Quality:" 
               VerticalAlignment="Center"
               Padding="0"/>

        <TextBox Grid.Row="2" Grid.Column="2" 
                 x:Name="txtQuality"
                 HorizontalAlignment="Left"
                 Width="45"
                 Height="28"
                 VerticalAlignment="Center"
                 VerticalContentAlignment="Center"
                 Padding="8,0"
                 Text="95"/>

        <!-- DPI -->
        <Label Grid.Row="4" Grid.Column="0" 
               Content="DPI:" 
               VerticalAlignment="Center"
               Padding="0"/>

        <TextBox Grid.Row="4" Grid.Column="2" 
                 x:Name="txtDPI"
                 HorizontalAlignment="Left"
                 Width="58"
                 Height="28"
                 VerticalAlignment="Center"
                 VerticalContentAlignment="Center"
                 Padding="8,0"
                 Text="300"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="btnCancel"
                    Content="Cancel" 
                    Width="90" 
                    Height="25"
                    Click="onExportExit"
                    Margin="0,0,8,0"/>

            <Button x:Name="btnExport"
                    Content="Export" 
                    Width="90" 
                    Height="25"
                    Click="onExportJPG"
                    Style="{DynamicResource Lab.Button.Accent}"/>
        </StackPanel>

    </Grid>
</Window>
